import pandas as pd
from sklearn.model_selection import train_test_split
from sklearn.feature_extraction.text import TfidfVectorizer
from sklearn.ensemble import RandomForestClassifier
from sklearn.metrics import classification_report

# 1. Load the dataset (referencing the structure in [cite: 21])
# df = pd.read_csv('tickets.csv')

# 2. Text Preprocessing (Simple Example)
def preprocess_text(text):
    return text.lower().strip()

# Combine Subject and Description for better context
# df['full_text'] = df['Subject'] + " " + df['Description']
# df['cleaned_text'] = df['full_text'].apply(preprocess_text)

# 3. Feature Extraction
tfidf = TfidfVectorizer(max_features=5000)
# X = tfidf.fit_transform(df['cleaned_text'])
# y = df['Category']

# 4. Model Training
# X_train, X_test, y_train, y_test = train_test_split(X, y, test_size=0.2)
# model = RandomForestClassifier()
# model.fit(X_train, y_train)

# 5. Evaluation [cite: 35]
# predictions = model.predict(X_test)
# print(classification_report(y_test, predictions))
